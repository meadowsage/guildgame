<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.meadowsage.guildgame.mapper.QuestOrderMapper">

    <resultMap id="questOrderMap" type="com.meadowsage.guildgame.model.quest.QuestOrder" autoMapping="true">
        <id column="id" property="id"/>
        <association property="rewards" columnPrefix="r_" autoMapping="true"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO quest_order (quest_id, person_id, state, rewards)
        VALUES (#{questId}, #{personId}, #{state}, #{rewards.value})
    </insert>

    <select id="selectAll" resultType="com.meadowsage.guildgame.model.quest.QuestOrder">
        SELECT *,
               rewards as r_value
        FROM quest_order
        WHERE quest_id = #{questId}
    </select>

    <update id="update">
        UPDATE quest_order qo
        SET state = #{state}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE
        FROM quest_order qo
        WHERE id = #{id};
    </delete>
</mapper>